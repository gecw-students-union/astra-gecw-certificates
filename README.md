# astra-gecw-certificates

Certificate repository managed by GCert.

This repository stores verified certificates and templates issued through [GCert](http://localhost:3000).

## Verification
Every certificate in `certificates/` is an immutable git commit. Modifications or deletions are tracked via git logs.
